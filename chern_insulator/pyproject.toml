[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "chern_insulator"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
chern_insulator = "chern_insulator.main:main"